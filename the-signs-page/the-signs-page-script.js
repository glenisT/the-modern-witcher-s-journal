var elementIsClicked = false;
console.log("FALSE") // declare the variable that tracks the state
function clickHandler(){ // declare a function that updates the state
  elementIsClicked = true;
  
}

function rotateOnClick()
{
    var iconAard = document.getElementsByClassName("cell-aard");
    if(iconAard.clickHandler() == true)
    {

    }
}